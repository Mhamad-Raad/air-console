# Leave these empty in dev — the frontend derives the backend URL from
# window.location.hostname automatically, so the same build works on
# localhost AND on a phone hitting your LAN IP.
#
# Set them explicitly only for staging/prod:
# VITE_API_URL=https://api.example.com
# VITE_SOCKET_URL=https://api.example.com
